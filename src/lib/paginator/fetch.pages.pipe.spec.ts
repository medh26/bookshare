import { FetchPagesPipe } from './fetch.pages.pipe';

describe('FetchPagesPipe', () => {
  it('create an instance', () => {
    const pipe = new FetchPagesPipe();
    expect(pipe).toBeTruthy();
  });
});
